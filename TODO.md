# TODO

- [ ] make sure that all registry dependencies are verified, to their catalog
    - [ ] shadcn entries, without registry prefix
    - [ ] download registry.json file for the registry and search for the imported parts
    - [ ] find out where base-ui registry is defined (for shadcn components)
    
